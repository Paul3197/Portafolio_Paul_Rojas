# Project screenshots

Drop your screenshots here with these exact names:

- `proyecto-1.jpg` — ERP / electronic invoicing module
- `proyecto-2.jpg` — Financial information platform

Then swap `ProjectPlaceholder` for a `next/image` `<Image src={project.imagePath} .../>` in
`src/components/selected-work.tsx`.
